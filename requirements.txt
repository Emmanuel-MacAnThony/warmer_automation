# Pinned to the versions that actually run locally — exact pins avoid pip
# resolver backtracking (loose floors + a stale pydantic pin caused multi-minute
# build hangs on deploy). Only langgraph + langchain-core are used; the full
# `langchain` / `langchain-openai` meta-packages were unused and removed.

# Core
python-dotenv==1.2.1
pyairtable==2.3.3
openai==2.17.0
requests==2.32.5
apify-client==1.7.1
typing-extensions==4.15.0
tenacity==9.1.4

# Backend API
fastapi==0.128.5
uvicorn[standard]==0.40.0
pydantic==2.12.5
python-multipart==0.0.29
pypdf==6.11.0
python-docx==1.2.0

# Database (Postgres / pgvector driver)
asyncpg==0.31.0

# LangGraph (segmentation pipeline) — only these two are imported
langgraph==1.0.8
langchain-core==1.2.9
langsmith==0.6.9

# Gmail OAuth
google-auth==2.53.0
google-auth-oauthlib==1.4.0
google-api-python-client==2.196.0

# MX preflight (bounce auto-detection layer 1) — domain DNS lookup
dnspython==2.8.0

# Testing (dev only — not needed at runtime)
pytest==9.0.3
