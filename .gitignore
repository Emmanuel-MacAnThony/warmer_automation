# Environment variables
.env

# Secrets — OAuth client credentials (load via GOOGLE_CREDENTIALS_JSON env var)
google_credentials.json
*google_credentials*.json
backend/google_credentials.json
*client_secret*.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
venv_312/
env/
.pytest_cache/

# Node (frontend deps/build — client also has its own .gitignore)
node_modules/
dist/
build/

# Logs
*.log
enrichment.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test output files
scraped_profile.json
scraped_tweets.json
crunchbase_profile.json
twitter_analysis.json

# Runtime data output (regenerated by the app; not needed in the repo)
data/
workspace/data/

# Stray root files / media
/*.jpg
/*.jpeg
/*.png
backend.log
/chrome-extension/

# Internal scripts/docs — not for the deploy repo
/workspace/
